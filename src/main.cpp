#include "instruction.hpp"
//#include "isa.hpp"
#include "execute.hpp"


int main (int argc, char *argv[])
{
    Instruction instr{};

    while (true)
    {
        //instr = readmem
        //execute(instr);
    }


    return (0);
}
