#include "soc.hpp"


template<typename T, typename M>
SoC<T, M>::SoC(size_t len) :
    _cpu(), _memory(len)
{
    //
}

template<typename T, typename M>
SoC<T, M>::~SoC(void)
{
    //
}


template<typename T, typename M>
void SoC<T, M>::run (void)
{
    //
}
